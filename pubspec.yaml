name: flutter_modify_image
description: A sample command-line application.
version: 1.0.0
# repository: https://github.com/my_org/my_repo

environment:
  sdk: ">=3.0.0 <4.0.0"

# Add regular dependencies here.
dependencies:
  # path: ^1.8.0
  crypto: ^3.0.6
  flutter_image_compress: ^2.4.0
  image: ^4.5.2
  webp: ^1.0.0+1

dev_dependencies:
  lints: ^4.0.0
  test: ^1.24.0

# 配置标识为包的可执行文件
executables:
  flutter_modify_image: flutter_modify_image